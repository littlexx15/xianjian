# 鲜剪 · 使用说明

打开后只有两件事：放素材，点生成。

1. 把视频拖进四个格子，或点「添加」。
   - **人物出镜**：打招呼、人设
   - **水果特写**：切开、特写
   - **产品展示**：装箱、摆拍
   - **人物口播**：讲价格、促下单
2. 选好保存位置和条数，点「开始生成」。
3. 想让每条看起来不太一样，勾选「每条成片略有画面差异」。
4. 生成后可到「剪辑」页：拖到要剪的位置 → 切开 → 删掉不要的一段 → 导出。

建议至少放两类素材。只有一类时，程序会再确认一次。

成片尺寸和音量在右上角「设置」里，一般不用改。

请把 `ffmpeg.exe` 和 `ffprobe.exe` 放到 `鲜剪.exe` 同一目录。
